# 技术栈
前端expo(react) -当前项目只负责前端
后端hono(ts)+bun
数据库supabase

# 前端和后端信息透明,通过D:\develop\shared\community-docs对接接口，共享信息

# Expo 社区应用
这是一个expo的多端项目

## 开始

1. 安装依赖：`pnpm install`
2. 启动应用：`npx expo start`

在 `app` 目录中开发，使用文件路由。
